# environments/prod.yaml
# Auto-updated by GitHub Actions CI via devops-central
# ArgoCD requires manual sync to apply changes

appEnv: production

pickup:
  tag: prod-385b800
  replicaCount: 2

delivery:
  tag: prod-9e2e3f8
  replicaCount: 2

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
