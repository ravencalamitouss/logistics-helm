# environments/prod.yaml
# Auto-updated by GitHub Actions CI via devops-central
# ArgoCD requires manual sync to apply changes

appEnv: production

pickup:
  tag: prod-1665e9d
  replicaCount: 2

delivery:
  tag: main-latest      # auto-updated when logistics-delivery merges to main
  replicaCount: 2

resources:
  limits:
    cpu: 200m
    memory: 256Mi
  requests:
    cpu: 100m
    memory: 128Mi
